{
  "name": "insights-ltw-playwright-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "test": "cucumber-js --require step_definitions/**/*.js --require utils/**/*.js --format progress features/**/*.feature",
  "playwright:install": "npx playwright install",
  "playwright:codegen": "npx playwright codegen",
  "test:headed": "cross-env HEADLESS=false npm test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1"
  }
}
